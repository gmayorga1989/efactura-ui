# /etc/nginx/sites-available/efactura-ui
# SPA eFactura (archivos estáticos en /opt/efactura-ui/html)
#
# DNS: efactura.appluxora.com → IP del Droplet
#
# En el servidor:
#   sudo mkdir -p /opt/efactura-ui/html
#   sudo cp /opt/efactura-ui/nginx.conf.example /etc/nginx/sites-available/efactura-ui
#   sudo ln -sf /etc/nginx/sites-available/efactura-ui /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
#   sudo certbot --nginx -d efactura.appluxora.com

server {
    listen 80;
    listen [::]:80;
    server_name efactura.appluxora.com;

    root /opt/efactura-ui/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
}
